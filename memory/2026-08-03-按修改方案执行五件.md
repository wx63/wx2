# 2026-08-03 · 按修改方案执行 P0/P1/P3/P5/P8 五件

依据 `C:\Users\userz\Desktop\跨境电商智能体系统修改方案.txt`，按我砍到最小的 5 件执行（便宜且高价值）。

## 1. .env.example + README.md（P0）
- `.env.example`：PORT/NODE_ENV/CORS_ORIGIN/SESSION_SECRET/OPENCLAW_* 全字段
- `README.md`：项目定位/启动/前端访问/后端端口/OpenClaw 配置/常见问题
- `.gitignore` 补 `.env`、`logs/`、`server/data/*.db`、`.diag*.js`

## 2. Express 托管前端静态（P10 前置）
- `server/index.js`：`express.static(ROOT_DIR)` + 非 /api 路径回退 index.html
- `app.js`：`API_BASE = ""`（同源相对路径）
- CORS 改成按 `CORS_ORIGIN` 白名单（同源后可不配）
- 收益：前后端同源，CORS / file:// fetch 限制一起消失；启动日志多一行"前端: http://localhost:3001/"

## 3. 审批按钮 批准执行→批准草稿（P3 诚实问题）
- `app.js`：列表按钮 + 弹窗按钮 "批准执行"→"批准草稿"
- 活动流文案 "已执行"→"已归档（执行器未接入）"
- Toast "已落库执行"→"已归档（执行器尚未接入，未真实执行）"
- 统计卡 "已执行/已发送"→"草稿已归档"
- `server/index.js`：新增 `POST /api/approvals/:id/execute`，返回"执行器未接入，仅完成审批归档，未真实执行"

## 4. 前端输出 escapeHtml（P5，当前真 bug）
- `renderConsole`：s.text/s.tag/s.label 全 escapeHtml（上一轮把产出从 200 字截断改成完整显示，放大了 XSS 面，这轮补上）
- `renderFeed`：time/tag/text escapeHtml
- `renderKBList`：文件名/meta/ext escapeHtml
- `renderLeads`：id/channel/name/country/msg/intent/time escapeHtml
- `renderReports`：time/tag/title escapeHtml
- 审批弹窗本就 escapeHtml（方案已确认对）

## 5. 命令执行落库 SQLite（P8 雏形）
- `server/db.js`：node:sqlite（Node 24 内置，无需 npm install），`commands` 表
- `bridge.js`：runDirect/runViaGateway 返回 `_meta`（path/model/promptTokens/completionTokens）
- `server/index.js`：`/api/command` 落库（command/agentId/sessionId/model/path/durationMs/status/contentLen/error/needsApproval/approvalId/promptTokens/completionTokens），失败也记
- 新增 `GET /api/commands` 查最近 N 条
- 错误信息脱敏：前端只回"内部错误"，不抛 e.message；error 落库只存首段

## 验证
- 静态托管：`GET /` → 200 text/html 11881 字节；`/app.js` → 200 application/javascript
- 同源 API：`POST /api/command` 相对路径 fetch 正常
- 落库：1+1 → id=1 path=direct dur=715ms status=ok；业务题 → dur=12949ms；发帖 → appr=AP-007
- execute 接口：未批准 → 400；批准后 → 200 返回"执行器未接入"
- XSS：含 `<script>` 的指令经 escapeHtml 原样输出，不执行

## 未做（留待后续，按方案优先级）
- 真鉴权（P1）：本机单用户不急，CORS 已收口
- SQLite 迁移 approvals/leads/reports（P2）：渐进，先 commands 验证通路
- 向量库（P6）：先持久化索引 + 测试
- Docker/部署（P10）：闭环稳了再说
