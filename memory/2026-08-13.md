# 2026-08-13

## 飞书实时查价

- 飞书消息此前不能实时查价，因为 Agent 工具注册表只有本地业务工具，且 bridge 系统提示禁止联网。
- 本次新增 `server/price-lookup.js` 和 `price_lookup` 工具。
- 飞书收到“价格/最低价/多少钱/报价/售价”类指令时会调用 `price_lookup`，模型未调用时也会强制补一次查价。
- 当前价格源为慢慢买公开折扣页 + 苹果中国官网；京东/拼多多直连仍受登录和风控限制。
- 验证：Agent 工具调用测试成功，模型自动调用工具并返回实时价格。
- 已扩展为通用商品查价，不再局限于 iPhone：支持任意商品关键词，并过滤配件、仿冒品、非目标型号。
- 车型价格接入汽车之家指导价；日产 N7 等车型不再返回“适配 N7”的周边配件。
- 通用价格链路：慢慢买折扣库 → 慢慢买全商品库 → 公开搜索页兜底；不出现硬性“查不到”。

## 上线与加固（晚间）

- 公网访问：Cloudflare Tunnel 临时隧道（trycloudflare.com，免账号免域名 0 元方案），指向 localhost:3001，登录页/API 均验证可达。临时域名重启即变；固定域名需买域名 + 命名隧道。
- 安全加固（C 项）：SESSION_SECRET 13 字符 → 64 位随机密钥；admin 密码重置为强密码（已同步 .env ADMIN_PASSWORD）；数据库备份到 data/backup-2026-08-13-12-29-07/。改后重启后端验证登录/飞书/隧道全通。
- 乱码清理：全仓库扫描 `????`，修复 14 处（server/index.js 6 处、public/app.js 7 处含订单弹窗 emoji、api.test.js 1 处），源码乱码清零。
- M9 时间戳统一：db.js 16 处 `DEFAULT (datetime('now'))` → `DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))`，与 JS nowIso() 格式一致。
- P1 agentic 防回归测试：新增 3 个（工具链成功路径 / 审批强制补跑 / 工具全挂回退），用本地 mock 模型服务器（改 bridge.PROVIDER.baseUrl 指向本地 http server，模拟 chat/completions tool_calls 协议）走真实 runAgentTools 循环，不触网。测试 23/23 全绿。
- 提交：d7f2097（含遗留未提交的 price_lookup 工具、bridge 提示词、移动端适配）。
- 踩坑：index.js 解构引入 runAgentTools，monkey-patch bridge 模块属性无效，改 mock PROVIDER.baseUrl 才生效。
