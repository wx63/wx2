# 2026-08-15 优化执行记录

## P0.1 Git 收尾

- 开始执行前本地 `master` 领先 `origin/main` 2 个提交。
- 工作区包含登录公开注册、Agent 结果富文本展示、订单序列/状态校验、测试与文档改动。
- `.env` 已在 `.gitignore` 中，确认不会提交。
- 计划按前端、server、文档分组提交，并把本地 `master` 快进推送到 `origin/main`。
- 已提交 3 组：docs、frontend、server；工作区干净。
- `git push origin master:main` 最终成功，远端 main 已更新到 `5aca5f1`。

## P0.2 清理遗留日志

- 删除根目录旧 `server*.log`，当前服务输出迁移到 `logs/out.log`、`logs/err.log`。
- 服务已用 `logs/` 目录重启，3001 端口在线，根目录已无 `server*.log`。

## P1 性能

- P1.1：`server/bridge.js` 直连请求改为 SSE 解析，支持内容增量与 tool_calls 增量；`/api/command` 支持 `stream: true`，前端用 `ReadableStream` 边收边渲染，JSON 轮询保留兜底。
- P1.2：`commands` 表新增 `prompt_cache_hit_tokens` / `prompt_cache_miss_tokens`，SSE usage 已收集并落库。
- P1.3：四张表已有时间/run 索引，`EXPLAIN QUERY PLAN` 均走 `USING INDEX`，无需重复建索引。
- P1.4：知识库 answer 取 3 块、Agent 预加载取 4 块且单块截断 1600 字符，满足阈值。

## P2 运维

- P2.1：server 本地安装 PM2，`start:prod` 已加入，`pm2 save` 已保存；Windows 登录自启用 HKCU `Run` 调用 `server/pm2-resurrect.ps1`。
- P2.2：PM2 日志固定到 `logs/out.log`、`logs/err.log`，`pm2-logrotate` 配置 50MB、保留 7 份。
- P2.3：新增 `server/backup.js`，每日 03:00 备份到 `data/backup/YYYY-MM-DD.db`，保留 7 份；手动触发已生成 `2026-08-15.db`。
- P2.4：scheduler 增加 try/catch、连续失败计数、audit 与可选飞书告警。

## P3 安全

- P3.1：登录增加 IP+邮箱内存失败锁，5 次失败锁 15 分钟，测试覆盖 429。
- P3.2：报告、订单、设置、飞书发送等写接口增加字段类型/长度/枚举校验，畸形 payload 返回 400。
- P3.3：飞书长连接只处理 text 消息、user 发送者，并支持 `FEISHU_ALLOWED_CHAT_IDS` / `FEISHU_ALLOWED_SENDER_IDS`；前端静态文件未发现 secret。

## P4 业务

- P4.1：Orders 真执行仍待外部平台凭据，保持只归档不造假执行。
- P4.2：查价新增 30 分钟内存缓存，慢慢买主源独立 try/catch，失败继续走苹果官网/360 搜索/汽车之家备用路径。
- P4.3：评估后维持 md 索引；PDF/docx/xlsx 继续仅上传不索引，后续可单独引入解析库。

## 收尾验证

- `npm test` 31/31 全绿。
- SSE 冒烟测试通过：`accepted,complete,done`。
- 服务由 PM2 管理并在线：`http://localhost:3001`。
- 修复过两个执行期问题：命令完成回调非 Promise 时 `.catch` 崩溃；SSE `finished` 置位过早吞掉 complete/done。
- P0.1 远端推送完成，`origin/main` 已同步。

## 指令控制台体验优化

- 控制台改为左右分栏：左侧“执行轨迹”只展示紧凑步骤摘要，右侧“最终产出”独立展示完整内容。
- 流式增量只刷新产出面板，不再每次重建整个控制台；首次出现产出时重建一次以更新状态头。
- 移动端自动切换为上下布局，步骤区和产出区分别内部滚动。
