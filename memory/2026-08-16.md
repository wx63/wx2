# 2026-08-16 P2 架构演进执行记录

## 完成项

- R1：`server/index.js` 按域拆分到 `server/routes/`，API 行为不变；认证、业务、知识库、命令、Agent 运行、审批、飞书路由各自独立。
- R2：工具注册表独立到 `server/tools/`，每个工具一个文件 + `registry.js` 单一注册表；`buildToolPrompt` / `runPlannedStep` 抽到 `server/tools/runtime.js`，本地路由与任务规划抽到 `server/planner.js`。
- R3：调度器抽到 `server/scheduler.js`，改用 setTimeout 精确排到下一执行点，避免 setInterval 分钟漂移；保留连续失败计数、audit 与飞书告警。
- R4：新增 `server/events.js` 进程内事件总线和 `/api/events` SSE；审批/活动/命令状态通过事件推送，前端 `EventSource` 实时刷新并保留 30s 轮询兜底；viewer 不会收到审批/命令事件。
- R5：新增 `server/executors/` 审批执行器适配器注册表（instagram/x/wechat/erp 占位），仍只归档、不真实执行对外动作；`/api/approvals/:id/execute` 返回适配器清单。
- R6：新增 `server/request-logger.js` 请求日志与慢请求审计，并给 `slow_command` 增加慢路径/兜底观测。

## 验证

- `npm test`：43/43 全绿（新增 SSE、执行器注册表、调度器精确排程测试）。
- `npm run test:frontend`：4/4 全绿。
- `node --check`：server、routes、tools、executors、public/app.js 全部通过。

## 备注

- `deploy/` 与 `docs/deploy-cloud.md` 是并发出现且不在本次改动内的未跟踪文件，提交时未纳入。
- 执行器仍为官方 API 接入前占位；批准只归档的设计保持不变。

## 生产部署修复：HTTP 会话 Cookie

- 远程 `http://106.55.18.244:3001` 登录返回 200，但响应没有 `Set-Cookie`，后续请求全部 401。
- 根因：生产模式原先固定 `cookie.secure: true`，express-session 在非 HTTPS 请求下不会下发 Secure Cookie。
- 修复：`SESSION_COOKIE_SECURE=auto` 默认按请求是否 HTTPS 自动决定；HTTP 直连可登录，HTTPS 反代仍会使用 Secure Cookie，也可显式设 `true` / `false`。
- 验证：本地 `NODE_ENV=production` + HTTP 下登录返回 `oc.sid`，`/api/auth/me` 200；`npm test` 与前端测试全绿。

## 远程页面问题修复：未登录根路径应跳转登录页

- 现象：`http://106.55.18.244:3001/` 未登录时显示空控制台壳，而不是登录页，看起来像页面没做好。
- 根因：`apiJson("/api/auth/me")` 对 `/api/auth/` 路径不主动跳转，`loadUser()` 只处理抛错，没有识别 `{ok:false}`，于是未登录时留在控制台空壳。
- 修复：`loadUser()` 在 `!data.ok || !data.user` 时调用 `redirectToLogin()`；前端冒烟新增“未登录根路径跳转登录页”回归测试。
- 验证：`npm run test:frontend` 5/5；`npm test` 44/44；更新包 `deploy/ecommerce-agent-update-20260816-1349.zip` 不含 data。
