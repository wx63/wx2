# 2026-08-17

## API Token 机器认证完成
- `server/index.js` 新增 `apiTokenAuth`，支持 `Authorization: Bearer <API_TOKEN>` 和 `X-API-Token`，认证后以 admin 机器身份访问。
- `sameOriginWriteGuard` 对机器身份请求放行，n8n 跨源写请求不会被 CSRF 守卫拦截。
- `.env.example` 新增 `API_TOKEN` 配置项；生产 `.env` 不提交、不入部署包。
- 测试：新增 api-token.test.js（Bearer、X-API-Token、错误/缺失 token、跨源写请求、admin-only 路由）与 api-token-unset.test.js（未配置时行为不变）。
- 验证：`npm test` 154/154 全绿。
- 云端代码已部署：`/opt/ecommerce-agent` 已更新，PM2 online，3001 监听，健康检查 401。
- 远端 `.env` 尚未配置 `API_TOKEN`；待手工执行：生成 `openssl rand -hex 32`，追加 `API_TOKEN=<值>`，再 `pm2 restart ecommerce-agent --update-env`。
